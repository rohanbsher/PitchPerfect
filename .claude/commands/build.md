---
description: Build the app and check for errors
---

Build the PitchPerfect app and verify no errors:

1. Run: `cd /Users/rohanbhandari/Desktop/Workspace/PitchPerfect && npx expo export --platform web 2>&1 | head -50`
2. Parse the output for errors and warnings
3. If there are TypeScript errors, list them clearly
4. Report build status (success/failure)

$ARGUMENTS
